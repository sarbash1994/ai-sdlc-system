# Playwright QA Worker

QA automation is outside the MVP execution path, but the planned worker contract is:

1. build project
2. start server
3. run Playwright tests
4. generate report

The MVP keeps this directory so the full architecture shape is present without pretending QA is implemented.
